import 'package:flutter/material.dart';

class DataSelected {
  final String? title;
  bool? isSelected;
  final IconData? icon;
  final String? value;

  DataSelected({this.title, this.isSelected, this.icon, this.value});
}
